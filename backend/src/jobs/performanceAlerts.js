/**
 * Legacy job stub. Marks use `student` + `marksObtained` (not `studentId` / `score`).
 * Combined IA + attendance alerts run in `attendanceAlerts.js` (`checkLowAttendance`).
 */
export const checkLowPerformance = async () => {};
