# Dev: talk to the Express API directly (CORS is enabled on the backend).
# Fixes "404 on /api/..." when the Vite proxy to localhost misbehaves (common on Windows).
# Ensure the backend is running (default port 5000).
VITE_API_URL=http://127.0.0.1:5000/api
